{
  "name": "@aibootcamp-capstone/inference-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Host-side process that owns the Claude Agent SDK, per docs/adr/0001-inference-boundary.md. Exposes an HTTP endpoint the containerized app's sidecar adapter (lib/inference/adapters/sidecar.ts) calls -- never runs inside the container itself.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.270"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  }
}
